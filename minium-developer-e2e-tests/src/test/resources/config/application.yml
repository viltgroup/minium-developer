# Web Drivers

minium:
  webdriver:
    desiredCapabilities:
      browserName: chrome
    window:
      maximized: true

# Configuration that can be injected in your tests
  config:
    baseUrl: http://localhost:8080/

---
spring:
  profiles: styx-ci

minium:
  webdriver:
    url: http://styx:4444/wd/hub
    desiredCapabilities:
      browserName: chrome
    window:
      maximized: true

# Configuration that can be injected in your tests
  config:
    baseUrl: http://styx:8080/
